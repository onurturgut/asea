---
document_type: "examples"
document_id: "V01-C34-EX01"
chapter_id: "V01-C34"
---
# C34 Örnekleri

```js
console.log(0.1 + 0.2 === 0.3); // false
console.log(Number.isFinite(Infinity)); // false
console.log(Number.isSafeInteger(Number.MAX_SAFE_INTEGER)); // true
console.log(Math.floor(-1.2), Math.trunc(-1.2)); // -2 -1
console.log((1 / 3).toFixed(2), typeof (1 / 3).toFixed(2));
```

Her örnek representation, domain validity veya presentation sınırını ölçer.
