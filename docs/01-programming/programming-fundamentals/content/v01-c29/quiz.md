# Mini Quiz

1. JavaScript'te type neden variable adına ait değildir?
2. Dynamic typing “kuralsızlık” anlamına neden gelmez?
3. `typeof null` sonucu neden null'ın gerçek language type'ı değildir?
4. `NaN` hangi type'tadır ve nasıl kontrol edilir?
5. Explicit conversion neden otomatik olarak güvenli değildir?
6. `"5" + 1` ile `"5" - 1` neden farklı yollar izler?
7. `Boolean("false")` neden true üretir?
8. `"5" === 5` ile `"5" == 5` arasındaki mekanizma farkı nedir?
9. `Number("")` hangi riski doğurur?
10. `parseInt("40px", 10)` hangi contract'ta doğru, hangisinde yanlıştır?
11. Conversion öncesi ve sonrası hangi farklı şeyler doğrulanır?
12. Digitlerden oluşan bir identifier neden string kalabilir?
