---
document_type: "quiz"
document_id: "V01-C30-QZ01"
chapter_id: "V01-C30"
version: "0.1.0"
status: "Draft"
---

# C30 Mini Quiz

1. İki ayrı object literal aynı property'lere sahipse `===` sonucu nedir?
2. `const profile = {...}` ifadesinde `const` tam olarak neyi korur?
3. Runtime'da hesaplanan property key hangi notation ile okunur?
4. Eksik property ile own `undefined` property nasıl ayrılır?
5. `const b = a` object'lerde neden kopya değildir?
6. Mutation nedir?
7. Alias nedir?
8. `{ ...source }` nested object'ler için neden bağımsızlık garantisi vermez?
9. `Object.assign(target, source)` hangi object'i mutate eder?
10. Seçici immutable update'te hangi object'ler yeniden üretilir?
11. Factory neden `"30"` değerini sessizce number'a çevirmemelidir?
12. Bir object modelinin yalnız property listesi neden yeterli değildir?
