---
document_type: "quiz"
document_id: "V01-C31-QZ01"
chapter_id: "V01-C31"
version: "0.1.0"
status: "Draft"
---

# C31 Mini Quiz

1. Array'in `typeof` sonucu neden array doğrulaması için yeterli değildir?
2. `find` no-match durumunda ne döndürür?
3. `some` ve `every` hangi farklı soruları sorar?
4. `filter` callback output'unu yeni element olarak kullanır mı?
5. `map` output length'i input'a göre nasıldır?
6. `reduce` initial value hangi iki sözleşmeyi görünür kılar?
7. `sort` source array'e ne yapar?
8. `toSorted` hangi identity'yi değiştirir?
9. Spread array kopyası object elementlerini deep-copy eder mi?
10. `map` callback'indeki eksik `return` ne üretir?
11. Empty array için `some` ve `every` sonuçları nedir?
12. Mutation/non-mutation kararında hangi ölçütler incelenmelidir?
